# Outils de développement uniquement — jamais installés en production.
# Utilisé par la CI (build + lint + tests) et en local.
-r base.txt

pytest>=9.0.3,<10.0
pytest-django>=4.12,<5.0
pytest-cov>=5.0,<5.1

# Lint & formatage
ruff>=0.4,<0.5
